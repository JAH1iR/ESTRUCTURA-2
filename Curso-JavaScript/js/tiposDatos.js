        let nombre = 'Pedro';//variable tipo String
        let cantidad = 257;//variable tipo int
        let precio = 77.77;//variable tipo double
        let verdad = true;//variable tipo logica
        let nulo = null;// tipo null
        let persona = {nombre:'Sophia', apellido:'Loren', edad: 38, signo:'libra'};//arreglo tipo asociativo

        document.writeln(nombre);
        document.writeln('<br>');
        document.writeln(cantidad);
        document.writeln('<br>');
        document.writeln(precio);
        document.writeln('<br>');
        document.writeln(verdad);
        document.writeln('<br>');
        document.writeln(nulo);
        document.writeln('<br>');
        document.writeln(persona.nombre, ' ', persona.edad, ' ',persona.signo);